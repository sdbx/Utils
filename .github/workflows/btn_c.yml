name: Test for buntang C implementation
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run test
    runs-on: self-hosted
    defaults:
      run:
        working-directory: ./test
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - name: Build object program
        run: make all
        working-directory: ./btn/c
      - name: Build test maker
        run: gcc -o test_maker test_maker.c
      - name: Build host program
        run: ./test_maker .test/btn.test
      - name: Run test
        run: ./test ../btn/c/btn
        timeout-minutes: 5
